import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#000080', // Warna utama untuk tombol
        },
        secondary: {
            main: '#01ffff', // Warna sekunder untuk tombol
        },
    },
});

export default theme;